<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.model.dao.DongcodeDao">

	<select id="getSido" resultType="java.lang.String">
		select distinct sido_name from dongcodes
	</select>
	
	<select id="getGugun" resultType="java.lang.String">
		select distinct gugun_name 
		from dongcodes
		where sido_name = #{sido}
		and gugun_name is not null;
	</select>
	
	<select id="getUmd" resultType="java.lang.String">
		select distinct dong_name
		from dongcodes
		where sido_name = #{sido}
		and gugun_name = #{gugun}
		and dong_name is not null;
	</select>
</mapper>